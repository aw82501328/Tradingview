# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# uv
.uv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node.js (server-cdp)
node_modules/

# OS
.DS_Store
Thumbs.db

# Cache / temp
*.log
.cache/
.cursor/cache/
.sr-tune/

# 生成的趋势报告（每次运行 full_report.js 重新生成）
.cursor/skills/**/report.html

# dump_baseline 生成的对拍数据快照（可再生成，不入库）
py_chain/.after/
py_chain/.baseline/
py_chain/.baseline_*/

# 基础数据存储（SQLite，按品种累积的K线库，不入库）
data/bars.db
data/bars.db-wal
data/bars.db-shm

# 调试残留：probe/shot/截屏/中间 dump（一次性脚本与产物，不入库）
_probe*.py
_shot*.py
_shot*.png
_clear_marks.py
_draw3m_*.js
_dbg*.txt
_redraw*.txt
_*.png
bis_front*.json

# 调试残留：临时画笔/截图脚本与重启前信号快照（一次性产物，不入库）
tmp_*
webapp-signals-before-restart.json

# WEB 服务运行时信号日志（每次回测/监控追加，可清空重生成，不入库）
py_chain/signals.ndjson

# JS 对齐试验工作区（chan_core 对拍用，正式版在 py_chain/）
chan_strategy/

# 参数中心运行时状态（overrides-only，机器本地，可随时重生成，不入库）
py_chain/web/module_params.json

# 基础数据存储（SQLite，运行时生成，不入库；py_chain 目录下误生成同样挡掉）
py_chain/bars.db
py_chain/bars.db-wal
py_chain/bars.db-shm
